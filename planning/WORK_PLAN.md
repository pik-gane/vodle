# Multi-session work plan

This is the issue-value analysis and session plan the work since 2026-09-05 has
been following (originally drafted by an AI agent from the open issues; kept
here verbatim so later sessions can pick up where the last one stopped).
A status ledger follows at the end — update it when a session lands.

---

Most valuable issues in pik-gane/vodle and a multi-session work plan

Context found in the repo

vodle is mid-way through a major backend migration from CouchDB/PouchDB to Matrix (issue #293, PRs #294–#312, phases 1–16 complete, useMatrixBackend: true in both environments). Per SYNC_DEBUG_STATE.md and SESSION_SUMMARY_2025-02-17.md (now in matrix-migration/history/), real-time rating sync currently works only via a 15-second periodic-discovery fallback because matrix-js-sdk stops emitting state events after initial sync — root cause unknown. Meanwhile, the long-standing CouchDB sync-consistency bug family (#159, #161, #162, #163, #171, #251) remains open, and issue #292 contains the owner's concrete hardening plan for it.

Value ranking of open issues

Tier 1 — Strategic workstream (highest value):

    #293 MVP for porting to Matrix — the active migration. Remaining: root-cause the SDK incremental-sync event emission failure, fix option resorting on reload, federation/offline testing, migration tooling validation, optional Phase 17 CouchDB cleanup.
    #292 Critical consistency improvements for PouchDB caching/sync — owner-authored action plan that addresses the whole sync bug cluster (#159, #161, #162, #163, #171, #251) that plagues the production CouchDB path.

Tier 2 — Urgent user-facing bugs (high value, small scope):

    #273 Language selection empty, hindering login — blocks app use entirely; issue includes a suggested fix (default to English, never disable "next").
    #186 Wrong waps on approval results explanation page — urgent + "ready to implement"; misleads users about results.
    #251 Poll claims to have ended before deadline — urgent; likely resolved by #292 work.

Tier 3 — Enabler:

    #228 Set up proper testing — 346 migration tests already exist; extending coverage to the tally pipeline and sync layer de-risks Tiers 1–2. Milestoned for First Release.

Tier 4 — Quick wins / housekeeping:

    #98 Sorting options refresh — community PR #298 already open; just needs review/landing.
    #57 Dark theme — likely closable: PR #291 already merged a dark theme implementation.
    #214 simple formatting in details texts, #83 archive polls, #193 simplify first-time start — popular, "ready to implement" UX features.
    Translation issues (#277 Tamil, #61 Arabic, #60 French, #56 Hindi, #54 Chinese) — mostly handled via Weblate (open PR #281).

Session-by-session plan

Session 1 — Urgent production fixes + housekeeping (Tier 2/4)

    Fix #273: make login never blocked by an empty language list — populate from the i18n folder, fall back to English, root-cause the browser-language detection failure.
    Fix #186: show effective waps instead of proxy waps on the approval-results explanation page.
    Triage: review community PR #298 (fixes #98) and recommend merge/changes; verify #57 is satisfied by merged PR #291 and close it; close stale "OLD"-labeled issues (#11–#16) if confirmed obsolete.

Session 2 — Matrix real-time sync root cause (#293)

    Reproduce the two-client sync failure documented in SYNC_DEBUG_STATE.md (now matrix-migration/history/); instrument the SDK sync loop to determine why incremental syncs emit no RoomState.events (suspects: in-memory-only store, SDK v37.5.0 bug, runtime-joined room re-emission path).
    Fix the root cause (e.g., persistent IndexedDB store or SDK upgrade), keep the periodic-discovery fallback as a safety net, and fix the known "options not resorted after reload" issue.

Session 3 — CouchDB consistency hardening, part 1 (#292)

    Implement bootstrap ordering: complete cache bootstrap from PouchDB, record last_seq, start changes feed with since=last_seq only after bootstrap.
    Add the change-event coalescing batcher and replication watchdog with stall detection, restart logic, and sync-status exposure to UI/logs. This targets #159 (syncing never ends) and #171 (incomplete uploads).

Session 4 — CouchDB consistency hardening, part 2 (#292 → #161/#162/#251)

    Add conflict detection (_conflicts) with deterministic resolution plus a startup background scan.
    Make draft→running poll transitions transactional (confirm polldb writes before userdb deletes); mark optimistic cache updates pending-until-confirmed with rollback on failure.
    Centralize authoritative-DB routing for all get/set/del; verify final tally waits for completed replication (fixes #161, #162, and likely #251).

Session 5 — Testing infrastructure (#228)

    Extend unit coverage to the tally pipeline (poll.service.ts) and DataService sync logic using the existing InMemoryBackend; add regression tests for each bug fixed in Sessions 1–4.
    Stand up the existing e2e scaffolding (e2e/, test/ wdio) into a runnable smoke suite and wire tests into CI.

Session 6 — Matrix migration hardening & completion (#293)

    Offline/reconnect convergence testing, two-homeserver federation test, migration tooling (CouchDB→Matrix export) validation, E2EE for private polls, and the performance/security report the issue requests.
    Only after production confidence: Phase 17 optional CouchDB code removal.

Session 7 (optional) — UX features

    #214 simple formatting in details texts, #83 archive polls, #193 simplified first-time onboarding.

Rationale for this ordering

    Session 1 ships small, independent, user-visible fixes immediately (a login blocker is the worst class of bug).
    Sessions 2–4 attack the two competing backend workstreams in parallel tracks: Matrix must reach reliable real-time sync to ever replace CouchDB, while the CouchDB path serves production users today and its consistency bugs corrupt actual poll results.
    Session 5's tests lock in the fixes before the riskier migration-completion work in Session 6.
    Feature work (Session 7) comes last — new features on an unreliable sync layer would compound the consistency problems.

---

## Status ledger

Update this section when a session's work lands; keep entries short and point
at the PRs/commits that carry the detail.

| Session | Status | Where |
| --- | --- | --- |
| 1 — urgent fixes | **done** (2026-09-05) | PR #314 (merged): #273 language fallback, #186 waps fix, keyboard-rating resort; #273/#98/#57/#186 and the OLD-labeled #11–#16 closed. Regression tests added 2026-09-10 (`login.page.spec.ts`, `explain-approval.page.spec.ts`, `poll/poll.page.spec.ts`). |
| 2 — Matrix sync root cause | **done** (2026-09-05) | PR #315 (merged): the SDK was never at fault — the client was not joined to the voter rooms; option resorting after reload fixed (`onInitialScanComplete`, unit-tested 2026-09-10). Periodic-discovery fallback kept. |
| 3 — CouchDB hardening part 1 | **done** (2026-09-06) | PR #316 (merged): bootstrap-gated sync, coalesced change batching, replication watchdog. Stall status now also shown in the page headers (`replication_is_stalled`, 2026-09-10). |
| 4 — CouchDB hardening part 2 | **done** (2026-09-09) | PR #319 (merged 2026-09-09): conflict cleanup, transactional draft moves, centralized routing, ordered voter mutations, publication confirmation, guarded finalization. The targeted issues #159/#161/#162/#163/#171/#251 are still open on GitHub: closing them needs a production observation, which nobody has made yet. |
| 5 — testing infrastructure | **done** (2026-09-09) | PRs #319/#320/#321: CI workflow `.github/workflows/tests.yml` (build + full suite with real CouchDB and real Synapse services on every PR; empty known-failure baseline), real-CouchDB two-client suite, MaxParC tally-pipeline suite (which found and fixed three delegation bugs), wdio e2e smoke suite (`npm run e2e`). |
| 6 — Matrix hardening & completion | **done** (2026-09-10) except Phase 17 | PR #322 (merged): Synapse harness, two-client convergence spec, registration fix, offline queue wired, E2EE initialized. Completed 2026-09-10 on this branch: (a) two-homeserver **federation** harness (`scripts/test-matrix.sh` now starts two Synapse servers federating over TLS plus the guard bot) and `matrix-federation.spec.ts` — a poll created on one server is joined and voted on from the other, magic links carry the poll's origin server; (b) the alias-domain wart fixed (server_name from the user ID); (c) **encryption**: poll data, options, voter data under the poll password and user data under the user password (the CouchDB backend's protection restored; Matrix E2EE covers timeline events only), Matrix login with a derived password so the homeserver never sees the real one, the poll title kept out of the room name; (d) the **guard bot** actually enforces deadlines now (it watched an event type the app never wrote; the deadline is copied into voter rooms; spec proves server-side rejection after the deadline); (e) user data restored from the user room on login (second-device restore); (f) **migration tooling validated** against real CouchDB + Synapse (`migration-real-backends.spec.ts`) — six fidelity bugs found and fixed; (g) the **performance/security/migration report**: `planning/matrix-migration/MATRIX_PERF_SECURITY_REPORT.md`. Not done: Phase 17 (CouchDB code removal) — the plan gates it on production confidence, which is the owner's call; a federation *partition* test (needs the harness to cut the link mid-run). |
| 7 — UX features | **done** (2026-09-10); the guest-voting part of #193 landed in session 12 | #214 simple formatting (`**bold**`, `*italics*`, paragraphs; Ctrl-B/Ctrl-I in the draft editor; everything else escaped — `simple-format.ts`); #83 archive polls (Archived section in My polls, archived polls exempt from local deletion; community PR #254 ported with fixes, can be closed); #193 first-time start: the language question is skipped when the browser's language is offered (e2e-tested). The rest of #193 — voting without being logged in, privacy overlay, login as overlay — was decided by the owner on 2026-09-10 (silent guest login with later migration to a proper account); see Plan 2, D1. PR #298 was ported in Session 1 and can be closed. |
| 8, 9, 9b — Plan 2, track A (Matrix data path) | **done** (2026-09-10) | PR #335: A1 #324 options added to a running poll reach the others; A3 #325 server-confirmed close and final read, with the two-phase close of A5 #334; A4 #329 the federation partition test (proxy in the harness). A2 #326 and the bot's grace/quiet periods landed with PR #323. Next: track B (production readiness). |
| 10 — Plan 2, track B part 1 (production readiness) | **done** (2026-09-10) except the owner's decisions | This branch (after PR #335): B1 #327 registration tokens in the app, production-candidate rate limits and token-required registration in the test harness (CI validates them), the guard bot's health endpoint, `documentation/deployment/MATRIX.md`; B2 #331 retention purge by the bot and room leaving by clients. Owner: domain, token, TLS, database, backups, rehearsal poll. |
| 11 — Plan 2, track B part 2 (#333) | **done** (2026-09-10) | This branch: a second device finds the account's voter room; delegation events encrypted and read from the server; specs for both. B3 #328 needs the owner's decision. |
| 13 — Plan 2, A5 residual (#334) | **done** (2026-09-10) | This branch: the guard bot snapshots a voter room's vodle state right before closing it; a voter room on another homeserver is written again as the bot right after the close (a write that forked with the close on the voter's server is soft-failed on the bot's server, so the bot never sees that fork, while the voter's server drops the rating and its previous value — the bot's re-affirmed events win the resolution everywhere); every closed voter room is re-read at `RECHECK_DELAYS_MS` and on late events, and what state resolution dropped is written back (`guard-bot/recheck.js`, unit-tested with `node --test`; CI runs it). The federation spec reproduces the #334 fork deterministically with the partition proxy — the voter writes on hs2 while the bot closes the room on hs1 — and shows hs2 dropping the rating and then taking the bot's pre-close value; both servers end with it. |
| 12 — Plan 2, B4 + D1 (#330, #193) | **done** (2026-09-10) | This branch: credential changes move the user's data (Matrix: homeserver password change and re-encryption, or an account switch that hands the voter rooms over; CouchDB: documents re-written under the new identity), resumable after an interruption; a magic link opened without an account takes part as a guest (consent question only when the deployment has a privacy statement) and a later login moves the guest's votes and polls to the account; poll memberships and drafts now reach the Matrix user room, so a second device of an account knows its polls. Specs at all three levels (unit, real Synapse, e2e). |
| 15 — Plan 2, D1 rework (#193) | **done** (2026-09-10) | This branch: a magic link takes part as a guest silently and shows the poll page at once; with a privacy statement the consent question moves to the bottom of the poll page, with the sliders, "add option" and "delegate" disabled and no rating stored until it is answered (the CouchDB stores refuse writes meanwhile, the Matrix path keeps ratings local). Unit specs; the e2e spec checks that no question appears on the join page. |
| 16 — Plan 2, B1 deployment scripts (#327) | **done** (2026-09-11) | This branch: `deploy/` — `generate-secrets.sh` (`.env` with generated secrets and the host settings), `deploy.sh up|status|update|backup|logs|down` (Synapse configuration from `deploy/homeserver.vodle.yaml` with PostgreSQL, accounts and registration token through the admin API inside the container, the app image built with the token, TLS from the host's certificate files via `docker-compose.tls.yml`, the privacy statement and imprint served at `/site/`, smoke checks; idempotent, `DRY_RUN=1`), `backup.sh`, `reload-tls.sh`, runbook `deploy/README.md`. `matrix.server_name` in `environment.prod.ts` is the single source of the server name; `guard_bot_user_id` may stay empty (derived). Validated without a docker daemon: dry runs, `docker compose config`, the merged Synapse configuration loaded by Synapse 1.160, the admin helper against the local test homeserver; unit spec for the derived bot id. |
| 17 — the handover of the CouchDB deployment (#327) | **done** (2026-09-11) | This branch: `environment.handover` — `successor_url` on the deployment being retired (the "+" button, a new draft and the start of a draft show a notice naming the successor, with a button that goes there; running polls continue, drafts stay editable), `predecessor_url` on the successor (a note on "my polls"); i18n en/de; specs. `documentation/deployment/MATRIX.md` §6: the plan — parallel run under `matrix.vodle.it`, retire the CouchDB build with the option, wait 62 days, then redirect `app.vodle.it` rather than serving the Matrix build on the old origin (stale caches would create empty poll rooms). |
| 18 — a deployment not reached at its own name (#327) | **done** (2026-09-11) | This branch: `PUBLIC_ORIGIN` in `.env` — where browsers reach the deployment, when that is not `https://<server_name>`. Needed by the owner's host, which has no DNS record of its own (only an HTTP redirect to the provider's host name) and a port of its own. `matrix.server_name` may carry the port; the certificate check, the smoke checks, Synapse's `public_baseurl` and nginx's redirect from port 80 all follow the origin instead of the name, and a server name that is not the served name is reported with the `.well-known/matrix/server` it needs for federation. `deploy/README.md` has both cases, including the one where the name's web server is somebody else's: `FEDERATION=off` in `.env` then closes the homeserver to others (`federation_domain_whitelist: []`) and the name stays the operator's to keep. The production image also raises the build's V8 heap limit, without which the Angular build dies on a host with about 2 GB of RAM. The first real deployment then found three things no test had ever exercised, because CI built without AOT and every test used an absolute homeserver URL: a template error in the consent checkbox that only the production build type-checks (CI now builds `--configuration production`), an uncached `index.html` that let a browser keep a shell naming chunks a redeployment had replaced, and `homeserver_url: "/"`, which matrix-js-sdk cannot use as a base URL at all (`MatrixService.resolveHomeserverUrl`). The first real poll then found the fourth: `rc_room_creation`, which neither the deployment settings nor the harness set, so Synapse's default applied — a burst of 10 rooms and then one per 62 seconds. vodle creates one room per voter, so a poll of 50 came up with 10 voter rooms and filled in over an hour, the creator counting the voters it held locally while a newcomer saw only the rooms that existed. CI never met it because its polls have ten voters, exactly the default burst. Raising it moved the wall one layer down, to `rc_message`: publishing a poll writes a vid, a deadline and a rating per option in every voter's room, about 400 state events for a poll of fifty, against a burst of 100. Worse, a throttled rating was **lost**: `setVoterData` wrote through the client directly instead of the wrapper that retries, and a 429 is not a connection error, so it was neither retried nor queued — the creator's local cache still counted the voter the server had never heard of. Throttled writes are now retried with jitter and queued like offline ones, the queue does not charge them an attempt and starts waiting from the short end again after a write goes through, and the recommended `rc_message` is 20/1000. Reproduced and verified with `scripts/production-clickthrough.sh` at `VODLE_SIMULATED_VOTERS=50`: under the old limit the creator saw 52 and the newcomer 17 and climbing; under the new one both settle at 52. The same pattern was then found in the guard bot, which has a retry helper but did not use it for either of its closing writes: a throttled voter-room close cost that room a ten-minute retry round while it went on taking ratings past the deadline, and a throttled poll-room close left the "closed" marker unwritten, which is the event every client waits for before tallying (#325). Both now go through the helper. Synapse counts these limits per user, so real voters entering a poll never collide with one another; the two writers that collide with themselves are a poll published with simulated voters and the bot. Then the same pattern once more, in the one place where it cannot be repaired: `announceVoterRoom` sent without a retry and swallowed the failure, so a voter room whose announcement was refused stayed invisible to every other participant for good — the vote existed and was counted only on its own device. It is now retried and queued (`voter_announce` in the offline queue). That is what a poll showing 52 voters to its creator and 51 to a newcomer came down to. The archiving labels of #83 also existed only in English, so every other language showed the raw key; German added, the rest follow through Crowdin. |
| 18 — a click-through of the production build (#327) | **done** (2026-09-11) | This branch: `scripts/production-clickthrough.sh` builds the production configuration, serves it at its own origin with `/_matrix/` forwarded to the test homeserver (`scripts/production-serve.js`, the deployment's nginx rules including the cache headers) and drives it through a real browser (`scripts/production-clickthrough.js`): register against the homeserver, publish a poll, take the invitation link into a fresh browser profile, knock into the closed poll room, consent, vote, and check that both sides count the same voters. It replaces the plain production build in CI, so it costs no extra build. Written because five defects reached the first real deployment through the gap between the production configuration and the one the rest of the suite uses: an ahead-of-time template error, an uncached `index.html`, `homeserver_url: "/"`, `rc_room_creation`, and a poll page that threw before it had its poll. Driving Ionic needs two habits, both in the script's comments: focus a field explicitly before typing, and blur it afterwards, because Ionic hands the value to Angular's form control on blur. |
| 19 — a burst is the client's problem too (#327) | **done** (2026-09-11) | This branch: the second production poll of fifty showed the creator 52 voters and a newcomer 50, and the newcomer's log settled the question of where the difference lives — discovery found all 53 announcements and joined all 53 rooms, and the rooms themselves held 226 of the 260 rating events (`bridgedRatings: 226`, and the missing ones absent from the rooms' state, not from the reader). So the reads are right and the writes never arrived: the creator's own log has 574 `M_LIMIT_EXCEEDED` on rating writes. Session 18 made every such write retry and queue, which is why nothing is lost any more, but it left the burst itself: some 400 writes fired within a second empty the account's token bucket, and then all of them retry at the same moment against a bucket that is still empty — the retries are the load. `MatrixService` now paces: every write reserves a moment to start in, the moments are `writeIntervalMs` apart (50 ms, i.e. the 20/s the deployment settings allow), a refusal pushes a shared pause out for as long as the server asks and doubles the interval, and twenty accepted writes in a row halve it back. It is a start-to-start spacing rather than a lock, so no call can deadlock behind one it issued itself. The twelve writes that still went to the client directly — the voter's vid (both places), the deadline in a voter room, an option, the two delegation events and six power-level changes — go through the retrying wrappers now (`sendEvent` is new). A newcomer also joins the voter rooms six at a time (`forEachConcurrently`) instead of one after another: fifty of them took the best part of a minute, which is the other half of what the owner saw. The limits themselves were the remaining suspect — a homeserver generated before `deploy/homeserver.vodle.yaml` carried them keeps Synapse's defaults, one message per five seconds — so `deploy/deploy.sh`'s checks now read `rc_message` and `rc_room_creation` back out of the running configuration and say so when they are missing (against the `synapse` service — the first version of the check named a service that does not exist, so it would have reported a container it could not reach as a homeserver without limits; it now tells those two apart), and both the runbook and the deployment guide say how to check by hand. Two smaller things from the same report: a heading rendering as `mypolls.archived` is a *stale asset*, not a missing translation (the key has been in `en.json` since session 7) — assets that keep their name across releases, the translation files above all, were served without a `Cache-Control`, so a browser went on using the copy it had; they are revalidated now, like `index.html` since session 18. And `scripts/check-i18n-keys.js` (in CI) checks that every key a template asks for exists in English, the fallback language: it found one that does not, `mypolls.no-running-polls`, in a duplicate of the empty-section notice sitting in the wrong section, now removed. On the owner's question — can the rate limit not simply be changed — it can, and the pace is now theirs to set: `matrix.writes_per_second` (20, the figure `rc_message.per_second` recommends) is the client's sustained rate, so raising the server's limits without raising this would have changed nothing, and — the owner's next question, why 20 a second at all — the pacer now mirrors the *other* half of the server's bucket as well: `matrix.write_burst` (1000, like `rc_message.burst_count`) is how many writes go at once, and only past that does the rate apply. Its first version ignored the burst and spaced every write at 50 ms, which cost a 450-write publication about twenty seconds the homeserver had never asked for — `Ratelimiter.can_do_action` allows `burst_count` actions before the rate bites, draining at `rate_hz`, so with a burst of a thousand Synapse would have taken the lot. A refusal empties the client's bucket, since the server has just said its own is empty, and the jitter now rides on the shared pause instead of being slept through twice. Then, asked what the effective limit was and to raise it as far as it will go: the recommended settings for vodle's own work are now sized so that the limiter never engages — `rc_message` 1000/20000, `rc_room_creation` 200/5000, joins and invitations to match, with the client's pair (20000, 1000) in step — which holds a poll of several hundred voters outright; a poll of 500 is about 4500 events and 501 rooms, both inside the bursts. `rc_login`, `rc_registration` and `rc_registration_token_validity` are deliberately left where they were: those guard password guessing and drive-by account creation, where a limit is the point, and they have nothing to do with the delays. What this gives up — the server-side stop on a runaway client — is weighed in the template's own comment against what remains: token-gated registration, and a client that paces, retries and queues. and an account the homeserver exempts can set 0. Synapse's `override_ratelimit` admin API lifts the limits for one account without a restart and without loosening them for anyone else — a zero there means *not rate-limited*, which its own source notes covers every limiter, room creation and joins included, not only messages; documented in MATRIX.md §2 and the runbook. A refusal from any server still starts the client's spacing again, whatever the setting. |
| 20 — delayed, never lost, and visibly so (#327) | **done** (2026-09-11) | The owner's answer to session 19: whatever the limit, it should only ever have delayed a write, never lost one — the move off CouchDB was meant to be the end of sync losing things. Audited, and three ways a write could still be lost: (1) `setUserData`/`setPollData`/`setVoterData` queued only a write that failed with an unreachable server or a 429 and **rethrew everything else** into a caller that logs and forgets (`setv_in_polldb`'s `.catch`), so a 500, a gateway error or a write attempted before its room was joined was gone; (2) `processOfflineQueue` **discarded** an event after five attempts; (3) `enqueueOfflineEvent` dropped its oldest entry at a thousand. All three are closed: everything the server does not take is queued, the only write ever given up on is one refused for good (a closed or purged room — 403/404, `is_permanent_refusal`), and even that is counted and reported; a write that keeps failing goes to the back of the queue rather than blocking the ones behind it, with one pass per drain so the rotation terminates; writes for the same thing coalesce (`offlineEventKey`), which bounds the queue by a poll's voters times its options instead of by how often a slider moved, so nothing has to be thrown away to make room. A client that is not initialised at all still throws, in all three: that is a programming error, not a server that is busy. Above that sits a check that does not depend on knowing how a write went astray: `reconcileOwnRatings` compares what this device believes it voted (`ownRatings`, persisted, saved debounced so four hundred ratings cost a handful of writes to storage) against what the voter rooms actually hold, and re-writes the difference through the ordinary paced, queued path. It runs every fourth round of the existing 15 s voter-room discovery, about once a minute, and only when nothing is in flight or queued. The read side already converged this way: that same timer re-runs discovery, so a voter room whose join was refused is joined on a later round. And the voter can see it: `DataService.sync_pending` (writes in flight plus queued) turns a spinner in the poll page header, `sync_is_stalled` (waiting over 30 s, or a write that keeps failing) shows the warning sign that until now existed for the CouchDB backend alone — on Matrix nothing was shown at all. `getOfflineQueueStatus` gained `inFlight`, `oldestPendingAt`, `refusedCount` and `droppedCount`; the two getters the template calls are O(1), since they run in a change detection cycle. i18n en/de. The one case left open, and documented rather than papered over: a write in flight when the tab is killed, where the request may or may not have reached the server — `ownRatings` outlives the restart, so the reconciliation catches it the next time the poll is opened. |
| 21 — the thirty seconds before the poll list (#327) | **done** (2026-09-11) | The owner: loading the site took almost half a minute before the polls showed, and a poll that had ended was still listed as running. Six agents mapped the boot, read, lifecycle, SDK-configuration, caching and rendering paths, each finding checked by two skeptics. **The ended poll** (e59c8a6): `Poll.start_lifecycle()` is what ends a poll past its deadline and what sets the timer for one that is not, and for a restored poll it was only ever called from the PouchDB reconciliation — a branch guarded `!useMatrixBackend`. So on Matrix nothing started a restored poll's lifecycle: one whose deadline had passed stayed running for ever, never tallied, and one still running got no closing timer either. **The boot path**: `waitForSync` registered with `once`, which unregisters after the first sync event whatever it is, while only `PREPARED` resolved the promise — a first event of `SYNCING` therefore left it with nobody to settle it and the app sat on its own 30-second timeout, then reported a failed login. That is the half minute, and it is the shape of the report. Beside it: `createClient` was called with no `store`, so the SDK's memory store applied and every load did a full initial sync of all 52 rooms of a 50-voter poll (~450–550 kB) — now an `IndexedDBStore`, one database per account, cleared when the session ends so the rooms do not outlive it; the app logged in with the password on every load instead of resuming its stored token, leaving a new device on the homeserver each time (`resumeSession`, which refuses a token belonging to another account so an address change cannot resume the old one, #330); and `initialSyncLimit` asked for ten timeline events per room where vodle reads its options and announcements from `/messages` and its data from room state — now one. `MatrixService.init` turned out never to be called at all, so the service had no logger and the 'online' listener that replays the offline queue was never registered. Still open, and measured rather than guessed: the per-poll warmup, which for a 50-voter poll is about 62 round trips on one serial chain — including one `GET /state` per voter room inside a `for…of`, the poll room's timeline paginated four times by four functions that share no cache, a leftover diagnostic that fetches the whole poll room state and discards it, and `voterRooms` that is never rehydrated from storage so every load re-walks and re-joins every room — and the 5.4 MB crypto WASM awaited before the client starts, for an E2EE vodle does not use (its votes are state events, which Matrix E2EE never covers). Then the rest of the half minute, which was never one slow thing but the app doing a poll's whole work before showing anything. The start is local-first now (bc670f4): `DataService.matrix_ready` holds the login in the background and the restored caches render at once, so a write made before the session is up is queued rather than refused (`MatrixService.loginInProgress`). And a poll's *contents* — the options, the other voters' ratings, the delegations, which cost a room per voter to join and read — are fetched when the poll is opened, not for every poll the user is in at app start: `DataService.ensure_poll_loaded` (idempotent, a failure is not remembered as done) against a `connect_to_remote_poll_db` that now does only the room lookup, the poll's own data and its listeners, and `MatrixService.setupPollEventHandlers` split into the cheap half (`setupPollRoomHandlers`, one room lookup, no reads) and the expensive one (`startVoterSync`). A magic link still loads everything, since that is what it is for, and the poll page shows a spinner only when the device holds nothing of that poll yet. Two reads went from the network to the sync the client already has: `getAllPollData` reads the poll room's state from the SDK store (`pollRoomStateFromStore`, generalised from the voter-room one — a room is trusted only when its own vodle state is there, so an unsynced room is asked for rather than reported empty), and `discoverVoterRooms` puts a voter room this device joined in an earlier session back into its maps from storage instead of joining it again (`rememberedVoterRoom`), which on a reload of a 50-voter poll is fifty requests that no longer happen. The leftover diagnostic that fetched a poll room's whole state and threw it away is gone. Measured rather than asserted: `scripts/production-clickthrough.js` steps 14–15 time a reload — from `goto` to the poll appearing in the list, and from the click to every vote being shown — and count the `/_matrix/` requests in each window, with the list capped at `VODLE_LIST_MS_MAX` (10 s) so that a poll list going back to waiting for the homeserver fails CI. The poll room's timeline is also walked once instead of three times — the options, the voter-room announcements and the delegations all read it, each with a pagination of its own; `pollRoomTimeline(pollId, max_age_ms)` shares a walk younger than 15 s, and the periodic voter re-discovery, whose whole purpose is to see what has just arrived, keeps the default of a fresh one. And the 5.4 MB crypto WASM was served uncompressed and without a `Cache-Control`, so every load fetched the lot again: `application/wasm` is in `gzip_types` now and `.wasm` revalidates like the shell and the translations (a 304 on a repeat load; not `immutable`, because the file keeps its name across releases). **Measured, production build, against a real homeserver, a poll of fifty**: the poll list after a reload takes **515 ms and 0 `/_matrix/` requests**, and opening the poll shows the page after **463 ms** and all 52 voters after **482 ms and 12 requests**. Against the half minute for the list and the ~50 s join in the report. What is left, and deliberately not done without the owner: the crypto WASM is still awaited before the client starts, for an E2EE vodle's votes cannot use (they are state events, which Matrix E2EE never covers) — `matrix.enable_e2ee` would turn it off, but that is a security-shaped switch and the owner's to throw. |
| 22 — the poll that showed fifty of fifty-two (#327) | **done** (2026-09-12) | The owner's creator and guest console logs settled a count that had been blamed on lost writes since session 18, and it was a read defect. The guest discovered all 52 announcements and held all 52 voter rooms yet reported 51; the creator held 51 and reported 31, with 0 options. In neither log did `getRatings` ever run — `[getRatings] options count` and `[getRatings] Voter …` never print, only `Returning cached ratings`. Because the cache was not its own: `updateRatingCache` CREATES the `ratingCaches` entry, and it is called by the live rating handlers and by `retroactiveScanVoterRooms`, which feeds it every rating it finds in the SDK's sync store. `getRatings` saw a cache and returned it, so a poll showed whatever `/sync` had carried — 242 of 260 rating events for the guest, none for the creator. Session 21's reordering made it certain, `load_poll_contents` having moved the voter sync (and that scan) ahead of the warmup where the old code read first. A cache now answers only for a poll whose rooms were actually read (`ratingsScanned`, cleared by every invalidation and by discovering a room the last read did not cover), and — the correction that cost a voter in the click-through before it was caught — **the read adds to what is known rather than replacing it**: a rating is only ever set, so the union of what was known, what the read found and what arrived while it ran cannot under-report. One level down, `voterRoomStateFromStore`'s test was unsound in the same way — a room's vid being in the store says nothing about its ratings being there — so the store's answer is checked against the poll's options and anything short is asked of the server, which costs a request only for a voter who has not rated everything. An empty option set is no longer cached either; that is what left the creator's session with a poll that had none. Verified: 210 unit specs including ones that reproduce an incomplete store, and the 50-voter production click-through at creator 52 / guest 52, the poll list in 479 ms and 0 `/_matrix/` requests. **And the silence**: the owner's report was eighty seconds with no console line whatsoever, and the screenshot proves it — two lines, and no 403 either, so the app had not reached its first `/login`. The app's logger runs at ERROR in a deployment, and session 21 had replaced raw `console.log` lines on this very path with `G.L.info`. Every stage of the start now says so unconditionally with the milliseconds since it began, from `DataService.init` and the storage restore (where the time must have gone) through the sync store, the crypto WASM, the login and syncing started; the crypto WASM is time-boxed at 20 s and the login at 90 s, so a magic link shows an error instead of spinning for ever (`MatrixService.within`). The cause itself is not yet known — three guesses were wrong (the guard bot's scan interval, which is only a backstop to its live knock handler; the knock wait; the WASM) — and the boot log is there to name it rather than a fourth. One more thing the same work turned up: a click-through killed before its trap ran left `environment.prod.ts` naming the test homeserver, and `git add -A` committed it; CI's own guard caught it, and the script now refuses to start on a dirty file and re-checks against git on exit. |
| 23 — the start that was slow for a reason nobody could see (#327) | **done** (2026-09-12) | CI reproduced the owner's slow start on its own, which is the point of session 22's boot log: a returning profile spent 18.4 s in `end-to-end encryption ready`, and the line before it says why — `the crypto store was rejected, clearing and retrying`. The check session 22 added asked the wrong question. It compared the store's recorded account against this one and cleared on a *mismatch*, so a browser with no marker at all — storage cleared, or any installation upgrading to the check for the first time — went down the expensive path it was written to avoid: a failed `initRustCrypto`, a `deleteDatabase` blocked on the connection that failed init still holds, and a schema migration of a store about to be thrown away. Ownership has to be proved, not merely not-disproved, so the store is now dropped whenever the marker is not exactly this account (`adoptCryptoStore`), and dropped directly by `deleteDatabase` before anything has opened it (`dropRustCryptoStore`) rather than through `clearStores()`, which would take the sync store with it and blocks in the one case that matters. The same run named a second defect in the guest's console, repeated some hundreds of times: `onRatingUpdate callback failed … Cannot read properties of undefined (reading 'name')`. A rating can arrive before the poll's `Option` objects exist — the live handlers are registered when the poll is opened, while the options are still being read — and `update_score` read `this.options[oid].name` for the oid it was handed, which in the incremental path comes straight from the event rather than from `this.oids`. The TypeError aborted the whole tally, so a poll's scores were left half-updated. Two guards: the bridge keeps such a rating in `poll_caches` without tallying it (`load_poll_contents` bridges every rating once the options are there), and `update_score` breaks ties by the oid instead of throwing. Two things about the test itself, which the owner had asked to be able to run without them. The failure report was 300 KB — every `/_matrix/` request of the run with its query string, one "Failed to load resource" repeated hundreds of times, and the Rust crypto tracing matrix-js-sdk turns on at Debug — with the one line naming the failure in the middle of it; it is digested now (`scripts/clickthrough-report.js`, with its own self-test in CI), so what is wrong is the line with the biggest count. And the boot log now starts before any of vodle's code runs: `index.html` stamps the page's arrival and `main.ts` prints what the bundle cost against it, because the symptom that started this — a join frozen for a minute with an empty console — cannot otherwise be told apart from a slow network. And the owner's own question — what are the PouchDBs for, if they are unused — had a second answer: every poll the user room named went through `ensure_local_poll_data`, which creates `local_poll_<pid>`, reads it three times (`info`, `allDocs`, `changes`) and holds the service un-ready until those reads of an always-empty database come back, on every start, on a backend where a poll's data lives in its room. `ensure_matrix_poll_known` does what such a poll actually needs — listed, a cache, a running lifecycle — and nothing else. **Measured afterwards, in the run of ff30ea6**: the returning profile's `end-to-end encryption ready` takes 129 ms where it took 18453 ms, the store drop that made it possible costs 1 ms, and the slowest stage of that start is now the login itself at 1114 ms — a start of 2.1 s from the client being set up, against a ceiling of 15 s. The console dump that carried the tally's TypeError some hundreds of times is clean — what was left in it was the SDK's MatrixRTC session manager, which vodle has no use for and which subscribes to EVERY state event the client sees. vodle's ratings ARE state events, so a poll of fifty runs it some thousands of times a sync looking for voice memberships that are never there, and reports a state event for a room the client has not got yet through `logger.error`, which no log level suppresses. It is stopped after `startClient` now, along with the Sync listener that would restart it, written to do nothing quietly if a later SDK renames either. **Measured in a real browser afterwards** (the run of 9fac576, against the run of ff30ea6): the click-through's console-error digest went from 140 kinds to 16, and the nine biggest of the old ones — `Got room state event for unknown room`, 28 to 36 times each — are gone entirely; what is left is the expected 404/403/401 of registration, login and room-alias probing. The crypto stage across the three profiles was 87, 99 and 180 ms. |
| 24 — where the thirty-three seconds went (#327) | **done** (2026-09-12) | The stopwatch session 23 put in front of the bundle did its job on the owner's next join, and the answer was not where anyone had been looking. Their guest's start, on a link doing about 2 Mbit/s: **2.4 s** for the page and the bundle, **49 ms** for the whole of `DataService.init` (the ten seconds of session 22 are gone — the lazy user database and the per-poll PouchDBs were the whole of it), **4.9 s** for the login ladder and the registration, **8.9 s for the crypto WebAssembly**, 1.9 s for `initRustCrypto`, then 2.6 s to the first sync and 2.6 s more for the user data: 18.6 s before the poll room is even asked for. The 5.4 MB of WASM is a third of the start on its own, and it was being asked for only AFTER the login and the registration had finished — five seconds in which the link was carrying almost nothing. `MatrixService.fetchCryptoWasm()` is now a memoised fetch that `DataService.init` starts at the start of the start and `initializeWithToken` awaits, so the download overlaps the login instead of following it; two specs pin the memoisation, since two fetches of 5.4 MB would be worse than the one serialised fetch this replaces. What this does NOT do is make the download smaller or optional, and that is the owner's call to make with the numbers now in hand: nothing vodle reads or writes needs Matrix E2EE — user data, poll data, options, ratings and delegations are *state* events, which megolm never covers, and the only room created with `m.room.encryption` is the user room, whose payloads are state events too — so `matrix.enable_e2ee: false` would take 10.9 s off every start of every guest and protect nothing that is protected today. |
| 25 — the vid defines the Matrix account (#327) | **done** (2026-09-12) | The owner asked what the privacy situation would be without end-to-end encryption, then asked how the homeserver could possibly link two polls of one person — and the answer was that the Matrix port had lost the CouchDB design. `connect_to_remote_poll_db` has always connected to a poll's database as `vodle.poll.<pid>.voter.<myvid>`: a separate account per poll, so the server holds nothing that ties two of a person's polls together. The Matrix port used ONE account per person — `@<hash of e-mail>` joined every poll room, created every voter room and sent every `m.room.vodle.voter.announce`, which carries the vid in plain text — so the homeserver, and every co-participant, could read the person behind two different vids straight off the sender field. The cut that restores it: **one `MatrixService` instance per identity**. Everything the service does to a poll room or a voter room already acts "as `this.client`", so giving a poll its own client is the whole of the change and the 129 places that say `this.client` never learn which account they are. `pollAccountName(pollId, vid)` and `pollAccountPassword(pollId, vid, userPassword)` mirror the CouchDB user name (the localpart hashed because a Matrix localpart may not carry the upper case a pid does; the password derived rather than reused, so a poll account cannot hand back the user's own); both are deterministic, so a second device reaches the same account from the vid it restores and nothing else. `MatrixService.forPoll` prefixes every stored key, and the sync store was already per account. `DataService.poll_matrix(pid)` signs that account in — resuming its own stored session, else logging in, else registering it the first time this device takes part — and all twenty-six poll-room and voter-room call sites go through it, while the injected service keeps the person's account and does the user room and nothing else. Two consequences settled in the same change rather than left as questions. **End-to-end encryption is off**, and the architecture decides it, not a preference: the SDK's crypto store is one per browser profile and belongs to ONE account, so a poll account could never have it — and it protected nothing, since user data, poll data, options, ratings and delegations are all *state* events, which megolm never encrypts, and the only room ever created with `m.room.encryption` was the user room, whose payloads are state events too. That also takes the 5.4 MB WebAssembly — 8.9 s of the owner's 33 s join — off every start. The user room's `m.room.encryption` now follows the flag, so no room is advertised as encrypted to a client that cannot encrypt. Two further consequences carried in the same change: a poll account's password is derived from the user's, so **a password change now carries every poll account with it** (`changePollAccountPassword`, called from `move_matrix_user_data` before anything leaves the old password behind) — the accounts themselves do not change, being named after poll and vid rather than the e-mail, so the rooms, the ratings and the tally are untouched, and an e-mail change alone needs nothing at all; and a spec against a real Synapse proves the property end to end — one person in two polls signs in as two unrelated accounts, a second device reaches the same account from the vid alone, and the person's own password does not open a poll account. |
| 26 — the polls from before, and the price of an account per poll (#327) | **done** (2026-09-12) | Two things the per-poll accounts of session 25 need to be true rather than merely implemented. **A poll from before belongs to the person's own account**: it created the poll room (power 100 until the poll starts) and the voter room (power 50), so the poll account joins both and may write to neither — every rating comes back 403, which the offline queue rightly takes for a refusal that will never be accepted, and the vote is simply gone. `MatrixService.takeOverFrom`, run by `open_poll_matrix` when a poll account is brought up, grants the poll account what the person's account holds in each room — the same handover an account switch does (`takeOverVoterRooms`), so the rooms, the voter id and the ratings already in them do not change and no tally moves. The gate is local and costs no request: unless THIS device stored the poll's rooms under the person's account there is nothing of theirs to take over, so a poll from after pays one storage read. Done once per (device, poll); a handover that fails is not recorded and is tried again, and after three starts it is given up on — a voter room the guard bot has closed keeps its power levels at 100 and would refuse the grant for ever. What it cannot repair is the past: the announcements the old account made, vid in plain text and its own user id as the sender, are in the poll room's history for good, so the unlinkability is a property of polls from here on, and the guide says so. **And the rate limits are an account matter now**: a device registers or signs in once per poll it takes part in rather than once in its life, and Synapse counts `rc_login.address` and `rc_registration` PER IP ADDRESS, so everyone behind one shared connection adds to the same bucket — a lecture hall opening one magic link at the same moment is 500 registrations from a single address, against Synapse's own burst of 5 and then one login per 5.5 minutes. Every login, registration and password change now waits a 429 out and tries again (they were the only calls left outside `retryOnRateLimit`), and both limits are sized for that case, burst 1000 and then 100 per second. `rc_login.failed_attempts` is the one exception and stays tight, because it is the only one of the three that guards anything a raise would weaken: it counts logins that got the password WRONG. And vodle stops producing those at all — `signInAs` asks `/register/available` first and registers rather than trying a login it expects to be refused, which as a by-product tells a wrong vodle password apart from a first join instead of turning it into `M_USER_IN_USE`. `rc_federation` was the last limiter left at its defaults and the only one that answers by SLEEPING — 10 requests per second per origin server, then 500 ms each, 3 concurrent, which paces a federated poll of 500 at about two rooms a second; now 500 per second and 20 concurrent, still per origin. The five limiters vodle never calls are listed in the template with the reason they stay put, and `deploy.sh`'s read-back now checks `rc_login` and `rc_federation` alongside `rc_message` and `rc_room_creation`. Also here: `poll_origin_<pid>` is no longer prefixed by the identity, since which homeserver a poll lives on is a fact about the poll and the poll account must see what the person's account learned from the magic link; and the live end-to-end-encryption spec, which CI caught failing because the flag it depends on is now off, turns it on for itself, which keeps the flag honest — what it proves is that switching it on still brings the Rust crypto backend up and encrypts a timeline event. Specs: the handover grants both rooms and records itself, grants nothing where the person only voted, runs once, is retried when it could not be finished, gives up on one that never can, and waits for the person to be signed in; the login and the registration go through the retry; and against a real Synapse, a poll made by the person's own account is taken over and voted in again from the poll account, in the same room, still one voter. 825 specs pass. |
| 27 — who can see what, written out (#327) | **done** (2026-09-12) | The owner asked for a summary of the privacy situation — who can see what and infer what about whom, delegations included. `documentation/PRIVACY.md`: the three kinds of account, the three kinds of room, what is encrypted with what, and then a party at a time — the homeserver operator, the guard bot, a co-participant, a magic-link holder, another homeserver, an outsider. Writing it against the code rather than from memory turned up two things the per-poll accounts do NOT hide, both of which the CouchDB backend shares, so neither is a regression and neither was known to be written down anywhere. **A person's user room names their polls**: user data is keyed `poll.<pid>.myvid`, `poll.<pid>.password`, `poll.<pid>.state`, and a vodle data key becomes part of the Matrix EVENT TYPE, which is never encrypted — so the accounts make two of a person's polls unlinkable to co-participants and to anyone reading the poll rooms, but not to the operator of the homeserver holding the user room (CouchDB: the same, in the user database's document ids `~<hash>§poll.<pid>.state`). **The login password is a fast derivation**: `deriveMatrixPassword` is one BLAKE2s, so an operator who records login requests can dictionary-attack a human-chosen vodle password and then read the user room; the derivation stops the password arriving in the clear, it does not make guessing it expensive. Delegations, which the owner asked for by name and which are disabled in both environments: the delegation graph BY VID is readable by the homeserver from the plaintext event types alone (`del_request.<did>` in the delegator's voter room, `del_response.<did>` in the delegate's), and `del_incoming.<did>` in the delegate's USER room ties their personal account to that poll — which today adds nothing the user room does not already say, and must be fixed together with it before delegation is switched on. Also recorded with a cost each: the join key is a single SHA-256 of a 64-bit poll password (about 58 GPU-years, or a couple of days for someone with a thousand GPUs and a reason), where the data itself is behind PBKDF2 at 600 000 iterations; IP address and timing correlate a person's poll accounts whatever the names say; the unsalted e-mail hash answers "does this person have an account here" one guess at a time. Nothing claimed here is new work — it is what the code does, checked line by line, with §8 saying what each fix would cost. Report §3.9 carries the two large ones, MATRIX.md §4 points at the document, and two stale claims found on the way were corrected: the deployment guide's example still set `enable_e2ee: true`, and the report's table still called the delegation events plain. |
| 28 — the join that froze, and the seventy seconds after it (#327) | **done** (2026-09-12) | The owner's next report came with a Firefox log carrying full HTTP timings, which settles what four rounds of guessing could not. **The freeze**: nine seconds between `the user cache is ready` and `credentials found, starting the backend`, with NOT ONE network request in them — a magic link opened on a device without credentials creates a guest first, and that is where the time goes. The code in `login_as_guest` and `login_submitted` reads as if none of it could cost anything, so each step now says so in the boot log, and the join page's "still waiting" notice starts its timer in `ngOnInit` at 3 s rather than when the poll is first asked for at 8 s — during the freeze the poll had not been asked for yet, so the timer that would have spoken had not been started. **The twelve seconds after the login**: `matrix_ready` resolved only after the whole user-data sync, and the join page waits for `matrix_ready`. For a guest with NO POLLS that was 12.8 s of creating a user room and filling it in front of a poll page that needed none of it — the vid and the password come from the link and this device's own storage. The sync now has a promise of its own (`matrix_user_data_ready`) and the poll starts at the login. Its writes also went out one after another, a round trip each, about thirty-five of them for a published poll; they now go eight at a time. **The stampede**: `getOrCreateVoterRoom` took its creation mutex BELOW the alias lookup, so every write for a voter — a vid, a deadline and one rating per option — paid its own 404 before finding out that another was already creating the room. The owner's log has five or six per voter, some two hundred and sixty for a poll of fifty. The mutex now covers the lookups too, registered synchronously before the first await. And the smaller ones from the same log: the voter's id was written 88 times for 52 rooms, because the state event takes a round trip to come back through the sync and until it did every caller saw an empty state — it is marked before the write now and un-marked if the write fails, and there is one place that writes it instead of two; a guest's brand-new credentials went through the three-rung login ladder, three refusals and three ticks of the one rate limit kept tight, before registering anyway — `login(..., account_is_new)` registers straight away; and voter rooms are joined sixteen at a time rather than six, since a deployment behind nginx speaks HTTP/2 and the browser's six-per-host limit was what the six was chosen against. 835 specs pass; the production configuration builds. What is left of the join, by the owner's own numbers: about 2 s of bundle, 2 s of login and registration, 2 s of first sync, 2 s for the poll account, a knock the guard bot has to answer, and the voter-room joins. |
| 14 — Plan 2, B3 (#328) | **done** (2026-09-10) | This branch: closed rooms. Poll rooms are `knock`-joinable and carry a join key derived from the poll password; the app knocks with a per-user HMAC proof, the guard bot verifies and invites (`guard-bot/knock.js`, node:test; the app's WebCrypto twin asserts the same vectors), and ignores anything else (a kick would let the knocker read the room's state as of their leave); voter rooms are `restricted` to the poll room's members. The two-client spec shows a client with the poll id alone kept out of the poll room and the voter rooms and one with a wrong password left standing at the door, seeing neither members nor state; the federation spec knocks across homeservers. |

## Plan 2 (2026-09-10): from "implemented" to "usable in production"

Everything in the plan above has landed except Phase 17 and the guest-voting
part of #193 (see the ledger). What the Matrix backend still lacks for a
production deployment without syncing issues was written up as issues
#324–#333 on 2026-09-10; this plan orders them. The tracks are independent
of each other, the sessions within a track are not.

### Track A — correctness of the Matrix data path (blocks "no syncing issues")

| # | Issue | What | Done when |
| --- | --- | --- | --- |
| A1 | #324 | **Done 2026-09-10** (session 8): the app's add-option path sends a `m.room.vodle.poll.option` timeline event (the room's state is locked once the poll runs, so the state event it wrote before was refused with 403 and never seen by anyone), and `setupPollEventHandlers` has a live handler for those events that updates the option cache and tells DataService, which registers the option like the CouchDB change feed does. Covers #163 on the Matrix backend. | The two-client spec: bob adds an option to the running poll, alice's cache and listener get it. Unit tests for both halves. |
| A2 | #326 | **Done 2026-09-10** (PR #323): the queue retries by itself with intervals from 1 s to 30 s and on the browser's `online` event; a connection failure during a retry no longer uses up the write's attempts. | The two-client spec's replay time went from 25–30 s to about a second (CI figure in the report, §6). |
| A3 | #325 | **Done 2026-09-10** (session 9): the guard bot closes a poll's voter rooms first and then writes the poll room's `m.room.vodle.poll.state` = closed (only it can, once the poll runs) before dropping the room's power levels. `Poll.end()` on Matrix waits for that event (`wait_for_matrix_poll_closure`, up to `closing.matrix_closure_timeout_ms`), reads the final ratings from the server (`reconcile_matrix_ratings`), stops syncing, tallies, and seeds a winner poll's lottery with the closing event's id (before, winner polls on Matrix never drew a winner at all). Voter rooms announced after the closing event are ignored by discovery. Without a guard bot the poll closes by convention after the timeout. | Unit tests for `Poll.end`, `getPollClosure`, the wait and the bridge; the two-client spec checks the closing order, the shared event id and identical final ratings in two clients. |
| A5 | #334 | **Done 2026-09-10** (session 13). A rating created in the same instant as the guard bot's closing power-level event forks with it and is dropped by state resolution, together with the previous value of that key (found 2026-09-10 by the two-client spec; issue has the evidence). Mitigated by the bot's grace and quiet periods (PR #323) and the two-phase close of A3 (session 9); **repaired** in session 13: the bot snapshots a voter room's vodle state before closing it; a voter room on another homeserver is written again as the bot right after the close, because a fork there is invisible on the bot's server (the late write is soft-failed) while the voter's server drops the rating and its previous value — the bot's events win the resolution on every server; every closed voter room is also re-read at `RECHECK_DELAYS_MS` (default 5 s, 1 min, 10 min) and when a late event arrives, and what was dropped is written back. The value the server had accepted last before the deadline is thereby the one that counts. A client's forked post-deadline write is still lost, by design. | Spec: a rating written before the deadline survives the close (two-client); the fork itself, made deterministic with the partition proxy, hs2 dropping the rating and both servers ending with the bot's pre-close value (federation spec). |
| A4 | #329 | **Done 2026-09-10** (session 9b): `scripts/federation-proxy.js` fronts the test servers' federation ports (Synapse listens on port + 10) with a control endpoint the spec uses to cut and heal the link; the harness sets Synapse's destination retry to 1–5 s (default 10 min). `matrix-federation.spec.ts`: both sides vote during the partition and see only their own vote, then converge after the heal. | Passes in CI; `federation_partition_heal_ms` in the report. |

Sessions 8, 9 and 9b (A1–A4) are done as of 2026-09-10, A5's repair in session 13: track A is complete.

### Track B — production readiness

| # | Issue | What |
| --- | --- | --- |
| B1 | #327 | **Done 2026-09-11** except the run on the host. Session 10: registration tokens supported by the app and required by the test harness; the harness runs the suite under the recommended rate limits (`documentation/deployment/MATRIX.md`); the guard bot got `GET /healthz` and a compose healthcheck; the deployment guide covers server name, Synapse settings, the bot, monitoring, backups, retention and a go-live checklist. Session 16 (the owner: "yes, please deploy", on the virtual server with existing certificates and privacy statement): the deployment is scripted (`deploy/`, runbook `deploy/README.md`) — the server name is `matrix.server_name` in `environment.prod.ts` (the one place; `deploy.sh` refuses the placeholder and a later change), `generate-secrets.sh` writes `.env` (database, admin, bot and token secrets of 190 bits each, mode 600, ignored by git), `deploy.sh up` generates the Synapse configuration and appends the production settings with PostgreSQL, registers the admin and bot accounts and the token through the admin API from inside the container, builds the app image with the token, mounts the host's certificate directory into the web container (`docker-compose.tls.yml`, nginx template, `reload-tls.sh` for renewals), serves the host's privacy statement and imprint at `/site/`, checks app, homeserver, well-known and bot, and is idempotent; `backup.sh` dumps the database and the keys. **Left to the owner**: running it on the host, and the rehearsal poll of the largest intended size to confirm the limits. |
| B2 | #331 | **Done 2026-09-10** (session 10): the guard bot removes a poll's rooms `RETENTION_DAYS` (default 365) after the deadline, through the Synapse admin API when it is an admin (`ADMIN_PURGE`), else by leaving; a client leaves and forgets the rooms of a poll it deletes locally. The retention period has to be named in the privacy statement (owner). |
| B3 | #328 | **Done 2026-09-10** (session 14), as the owner decided ("make it properly closed"): poll rooms have the `knock` join rule and carry a join key K = SHA-256("vodle-join:" + poll id + ":" + poll password) in their state; a joiner who holds the magic link knocks with HMAC-SHA-256(K, own user id) as the knock's reason, the guard bot verifies the proof against K and invites (a wrong proof is left unanswered — a kicked or departed knocker could read the room's state as of their leave, Synapse's departed-user rule), and the app joins once invited (`joinPollRoom`, `guard-bot/knock.js`, shared test vectors). Voter rooms have the `restricted` join rule (members of the poll room). Knowing the poll id alone gives neither the membership nor the ciphertext any more; magic links keep working, across federation too. Polls created before stay public. The bot is now required for joining, not only for closing. |
| B4 | #330 | **Done 2026-09-10** (session 12): `DataService.perform_user_data_move` — a committed credential change (login page, settings page) records the old credentials as a pending move and completes it: on Matrix a changed password is changed on the homeserver (`changePassword`, user-interactive auth with the derived old password) and the whole user room re-encrypted; a changed address means another account, which takes over the voter rooms (`takeOverVoterRooms`: joins, gets power 50 from the old account — same rooms, same voter ids, the tally does not change) and gets the data (`syncUserDataWithMatrix(force)`); on CouchDB the user documents are re-written under the new identity and the user db connected anew. The pending record survives a restart; every step is idempotent; given up after three failed attempts. The settings page commits e-mail and password on OK/enter instead of per keystroke. Found and fixed on the way: the poll membership keys (voter ids, poll passwords, drafts) were never written to the Matrix user room, so a second device of an account knew none of its polls (the report claimed otherwise); the login page's fresh-account registration ignored the registration token; `setu` sent the credentials to the user room. |
| B5 | #333 | **Done 2026-09-10** (session 11): a second device of one account now finds the account's voter room by its alias instead of creating another (it did, when its storage was fresh); delegation request/response events are encrypted under the poll password, decrypted for listeners, and read from the server's full timeline; the two-client spec covers both. Delegation stays disabled in both environments — enabling it is a product decision. |
| B6 | #159 #161 #162 #171 #251 | The CouchDB consistency fixes of sessions 3–4 need a production observation before these can be closed; whoever runs a production poll on the CouchDB backend should record whether the symptoms recur. |

Session 10: B1 + B2 — done 2026-09-10 (B1 except the owner's decisions). Session 11 (B5) — done 2026-09-10. Session 12: B4 together with D1 — done 2026-09-10. Session 14: B3 — done 2026-09-10. Session 16: the deployment scripts of B1 — done 2026-09-11. Session 17: the handover options for retiring the CouchDB deployment — done 2026-09-11. Open in track B: the deployment run itself on the owner's host (#327: test deployment, then the parallel production install, then the handover), and B6 (production observation).

### Track C — after production confidence

| # | Issue | What |
| --- | --- | --- |
| C1 | #332 | Phase 17: remove the CouchDB backend, the `useMatrixBackend` flag, the PouchDB dependency, the CouchDB docker files and docs. Only after a real group has used the Matrix backend in production for a while (Track B first). |

### Track D — user-facing work that does not depend on the backend

| # | Issue | What |
| --- | --- | --- |
| D1 | #193 | **Done 2026-09-10** (session 12, reworked in session 15 as the owner asked: "voting should be possible before registering"): a magic link opened on a device without credentials no longer leads to the login flow. The joinpoll page takes part as a guest right away, without any question, and goes on to the poll page; when the deployment has a privacy statement, the consent checkbox (the login page's text and link) sits at the bottom of the poll page, and until it is checked the sliders, "add option" and "delegate" stay disabled and no rating is stored (`DataService.consent_pending`, `Poll.set_my_own_rating`); once checked, the page stores its ratings. The login page stays as it is. A guest is a normal account with random credentials (about 115 bits; the old "Guest" + number scheme let anyone enumerate every guest account) that only the device knows; the poll page shows a banner with a login button, the settings page a note, the logout page a warning. Logging in later — from the banner, the login page or the settings page — moves the guest's polls, votes and settings to the account with the machinery of B4 (the voter id stays, so the tally does not change), then deactivates the guest account. Both backends. Specs: unit (`data.service.spec.ts`), against a real Synapse (`matrix-two-client.spec.ts`: the account changes the guest's vote in the same voter room, the guest's credentials are dead afterwards), e2e (`test/specs/guest-join.e2e.js`: the built app keeps a magic-link visitor on the join page). The guest account itself cannot be avoided (the Matrix backend needs an account to read the poll), but the visitor never sees or types anything for it; the login *as an overlay* is not wanted (owner, 2026-09-10). |
| D2 | #61 #60 #56 #54 | Translations via Weblate; a language is offered in the app once it is mostly translated (Tamil #277 was added this way; Arabic and French are still mostly untranslated). |
| D3 | PRs #285 #253 #202 #268–#270 | Open pull requests need a decision by the owner: #285 (weighted delegation and UI changes, 1767 files against the `hemped` branch — far too large to review as is; ask for a rebased slice or close), #253 (CSS theme option from 2023, probably superseded by the dark theme of PR #291), #202 (Finnish translation from 2022; Finnish is in the app, so probably superseded), the dependabot bumps #268–#270 (2024; the lock file still carries the old versions of `express`, `follow-redirects` and `ip`, so they are still applicable — merge them or bump the three with a fresh `npm audit`). Community PRs #254 and #298 were ported and closed only in 2026-09; a week from opening to a decision should be the rule. |
| D4 | backlog | The older "ready to implement" features (#84, #62, #86, #156, #173, #182, #184, #201, #169) stay unscheduled until Tracks A–B are done. |

### Process notes

- Land work in reviewable slices (PR #319 with ~5000 added lines was too large to review).
- Every fix comes with a spec; every claim of "works against a real server" with a spec that runs in CI (the real-server suites skip themselves without servers, and CI runs with `--no-skips`).
- Keep this ledger and `matrix-migration/MIGRATION_STATUS.md` current when a session lands; keep the historical documents in `matrix-migration/history/` untouched.

