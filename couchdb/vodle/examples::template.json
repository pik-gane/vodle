{
    ":COMMENT": "Template for example polls",

    "type": "Poll type: winner|share",

    "language": "Poll language (optional, defaults to creator's language setting)",

    "title": "Poll title",

    "desc": "Poll description (optional)",
    
    "url": "Poll's 'read more' URL (optional)",

    "due_type": "Poll closing date relative to starting date: custom|10min|hour|midnight|24hr|tomorrow-noon|tomorrow-night|friday-noon|sunday-night|week|two-weeks|four-weeks",
    "due_custom": "If due_type==custom, fixed poll closing date in ISO format",

    "options": [
        {
            "name": "Option 1 name (must be unique within the poll)",
            "desc": "Option 1 description (optional)",
            "url": "Option 1 'read more' URL (optional)"
        },
        {
            "name": "Option k name (must be unique within the poll)",
            "desc": "Option k description (optional)",
            "url": "Option k 'read more' URL (optional)"
        }
    ]

}
