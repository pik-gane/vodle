import { SortoptionsPipe } from './sortoptions.pipe';

describe('SortoptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortoptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
