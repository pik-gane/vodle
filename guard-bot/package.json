{
  "name": "vodle-guard-bot",
  "version": "0.0.1",
  "private": true,
  "description": "Matrix guard bot for vodle — monitors poll deadlines and closes rooms when they expire",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test recheck.test.js knock.test.js"
  },
  "dependencies": {
    "matrix-js-sdk": "^37.5.0"
  }
}
